{% extends "@FOSUser/layout.html.twig" %}



{% block fos_user_content %}
    {% trans_default_domain 'FOSUserBundle' %}

    <div class="login-box ptb--100">
        <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
            <div class="login-form-head">
                <h4>Réinitialisation mot de passe</h4>
                <p>
                    {{ 'resetting.check_email'|trans({'%tokenLifetime%': tokenLifetime})|nl2br }}
                </p>
            </div>
            <div class="login-form-body">
                <div class="form-gp">
                    <label for="username">login</label>
                    <input type="text" id="username" name="username" required="required" />
                    <i class="ti-user"></i>
                </div>
                <div class="submit-btn-area">
                    <button id="form_submit" type="submit">Envoyer <i class="ti-arrow-right"></i></button>
                </div>
                <div class="form-footer text-center mt-5"><!--p class="text-muted">Don't have an account? <a href="register.html">Sign up</a></p-->
                </div>
            </div>
        </form>
    </div>

{% endblock %}
